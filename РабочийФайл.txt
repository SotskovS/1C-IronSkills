1. Дополнительная колонка в форме списка "CRM_Интерес"
    - Добавить колонку "Контактное лицо" для того, чтобы видеть от кого задача


        - НаКлиенте // только на стороне клиента
        Процедура ПолучитьДанныеАктивнойСтроки()
            //ТекущаяСтрока - ключевой элемент списка(содержит ссылку на Документ)
            //ТекущиеДанные - данные которые отображаются в списке
            Элементы.СписокЗаказов = Элементы.СписокЗаказов.ТекущиеДанные;
            Сообщить("Номер " + ДанныеСтроки.Номер + "Ответственный " + ДанныеСтроки.Ответственный);
        КонецПроцедуры


2. При записи Документа "Телефонный звонок", программа выдает ошибку"Поле Абонент не заполнено" 
Приходится перевыбирать контактное лицо, тогда удается провести документ.
    - Нужно устранить необходимость перевыбора контактного лица в поле "Абонент"



Изменение ВЫДЕЛЕННЫХ элементов списка

НаКлиенте
Процедура ИзменениевыделенныхЭлементов()
    //Элементы,списокЗаказов - ВыделенныеСтроки(Массив - ссылки на заказы)
    МассовЗаказов = Элементы.СписокЗаказов.ВыделенныеСтроки;
    ИзменитьСтатусЗаказов(МассивЗаказов);
    Элементы.СписокЗаказов.Обновить();
КонецПроцедуры

НаСервереБезКонтекста
    Для каждого ТекЗаказ Из МассивЗаказов Цикл
        ДокОбъект = ТекЗаказ.ПолучитьОбъект();
        ДокОбъект.Статус = Справочники.СтатусыЗаказовКлиентов.Завершен;
        ДокОбъект.Записать();
    КонецЦикла;
КонецПроцедуры
